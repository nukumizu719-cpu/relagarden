---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// 施工事例をすべて取得し、日付の新しい順に並び替え
const allCases = await getCollection('cases');
const sortedCases = allCases.sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const pageTitle = "施工事例一覧";
const description = "リラガーデンが手掛けた愛知県岡崎市周辺の人工芝施工実績をご紹介します。雑草対策から子供の遊び場まで、お客様の理想を叶えた事例をご覧ください。";
---

<BaseLayout pageTitle={pageTitle} description={description}>
  <div class="bg-[#f9faf7] min-h-screen pb-20">
    <section class="bg-white border-b border-gray-100 pt-16 pb-12 mb-12">
      <div class="max-w-6xl mx-auto px-5 text-center">
        <h1 class="text-3xl md:text-5xl font-bold font-zen text-gray-900 mb-4">施工事例</h1>
        <p class="text-gray-500 max-w-2xl mx-auto leading-relaxed">
          「雑草に困っている」「子供が遊べる庭にしたい」<br class="hidden md:block" />
          そんなお悩みを解決した、リラガーデンの確かな施工実績です。
        </p>
      </div>
    </section>

    <div class="max-w-6xl mx-auto px-5">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 md:gap-10">
        {sortedCases.length > 0 ? (
          sortedCases.map((caseEntry) => (
            <article class="group bg-white rounded-3xl overflow-hidden shadow-sm hover:shadow-xl transition-all duration-500 flex flex-col">
              <a href={`/cases/${caseEntry.slug}/`} class="relative block overflow-hidden">
                <div class="aspect-[16/10] overflow-hidden">
                  <img 
                    src={caseEntry.data.image} 
                    alt={caseEntry.data.title} 
                    class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                    loading="lazy"
                  />
                </div>
                <span class="absolute top-4 left-4 bg-green-700/90 backdrop-blur-sm text-white text-[10px] font-bold px-3 py-1.5 rounded-full tracking-wider">
                  {caseEntry.data.area}
                </span>
              </a>

              <div class="p-8 flex-grow flex flex-col">
                <time class="text-[10px] text-gray-400 font-bold mb-3 tracking-widest uppercase">
                  {caseEntry.data.pubDate.toLocaleDateString('ja-JP')}
                </time>
                <h2 class="text-lg font-bold text-gray-800 mb-4 line-clamp-2 leading-relaxed font-zen group-hover:text-green-700 transition-colors">
                  {caseEntry.data.title}
                </h2>
                
                <div class="mt-auto pt-5 border-t border-gray-50 flex items-center justify-between text-xs">
                  <div class="flex flex-col gap-1">
                    <span class="text-gray-400 text-[9px] uppercase tracking-widest">Cost</span>
                    <span class="font-bold text-gray-700">{caseEntry.data.cost}</span>
                  </div>
                  <div class="flex flex-col gap-1 text-right">
                    <span class="text-gray-400 text-[9px] uppercase tracking-widest">Period</span>
                    <span class="font-bold text-gray-700">{caseEntry.data.period}</span>
                  </div>
                </div>
              </div>
            </article>
          ))
        ) : (
          <div class="col-span-full py-20 text-center">
            <p class="text-gray-400">現在、施工事例を準備中です。公開までしばらくお待ちください。</p>
          </div>
        )}
      </div>

      {/* 問い合わせフォームへの誘導エリア（LINEリンクから修正） */}
      <div class="mt-20 p-10 bg-green-800 rounded-[40px] text-center text-white shadow-2xl relative overflow-hidden">
        <div class="relative z-10">
          <h2 class="text-2xl font-bold font-zen mb-4">あなたのお庭も、劇的に変えませんか？</h2>
          <p class="text-green-100 mb-8 text-sm opacity-90">
            現地調査・お見積もりは無料です。まずはお気軽にお問い合わせフォームよりご相談ください。
          </p>
          <a href="/contact/" class="inline-block bg-white text-green-800 px-12 py-4 rounded-full font-bold hover:bg-green-50 transition-colors shadow-lg">
            無料見積もり・お問い合わせ
          </a>
        </div>
        {/* 装飾用背景要素 */}
        <div class="absolute top-0 right-0 w-32 h-32 bg-green-700 rounded-full -mr-16 -mt-16 opacity-50"></div>
        <div class="absolute bottom-0 left-0 w-24 h-24 bg-green-900 rounded-full -ml-12 -mb-12 opacity-50"></div>
      </div>
    </div>
  </div>
</BaseLayout>