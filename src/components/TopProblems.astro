---
---
<section class="problems-section">
  <div class="container">
    
    <h2 class="section-title-problem">
      <span class="decoration">＼</span>
      こんなお悩みはありませんか？
      <span class="decoration">／</span>
    </h2>

    <div class="problems-grid">
      <div class="problem-item">
        <span class="check">✔</span>
        <p>毎年雑草が生えてきて手入れが大変</p>
      </div>
      <div class="problem-item">
        <span class="check">✔</span>
        <p>子どもが安心して遊べる庭にしたい</p>
      </div>
      <div class="problem-item">
        <span class="check">✔</span>
        <p>庭の見た目をキレイにしたいけど維持が難しい</p>
      </div>
      <div class="problem-item">
        <span class="check">✔</span>
        <p>土や砂で家の中が汚れるのを防ぎたい</p>
      </div>
    </div>

    <div class="solution-banner">
      リラガーデンなら全部まとめて解決
    </div>

  </div>
</section>

<style>
  /* --- 基本設定 (PC) --- */
  .problems-section {
    padding: 80px 24px;
    background: #f7f7f7;
    overflow: hidden; /* 横スクロール防止の保険 */
  }
  .container {
    max-width: 1100px;
    margin: 0 auto;
    box-sizing: border-box;
  }

  /* タイトル */
  .section-title-problem {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 15px;
    text-align: center;
    font-size: 2.2rem;
    font-weight: bold;
    color: #333;
    margin-bottom: 50px;
    font-family: 'Zen Maru Gothic', sans-serif;
    white-space: nowrap; /* 折り返し禁止 */
  }
  .decoration {
    color: #2e7d32;
    font-size: 1.8rem;
    font-weight: normal;
  }

  /* 2列のグリッド構造 */
  .problems-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    max-width: 900px;
    margin: 0 auto;
  }

  .problem-item {
    background: #fff;
    padding: 24px;
    border: 1px solid #e5e5e5;
    border-radius: 8px;
    display: flex;
    align-items: flex-start;
    gap: 12px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.06);
  }
  .check {
    color: #2e7d32;
    font-weight: bold;
    font-size: 1.4rem;
    line-height: 1;
    flex-shrink: 0; /* アイコンが潰れないようにする */
  }
  .problem-item p {
    margin: 0;
    font-size: 1.15rem;
    color: #333;
    font-weight: 500;
    line-height: 1.6;
  }

  /* 解決バナー (PC) */
  .solution-banner {
    text-align: center;
    font-size: 2rem;
    font-weight: bold;
    color: #2e7d32;
    margin: 50px auto 0;
    padding: 20px 0;
    border-top: 3px solid #2e7d32;
    border-bottom: 3px solid #2e7d32;
    max-width: 700px;
    font-family: 'Zen Maru Gothic', sans-serif;
    background-color: #fff;
    white-space: nowrap; /* 折り返し禁止 */
    box-sizing: border-box; /* パディングを含めた幅計算にする */
  }

  /* --- モバイル対応 (700px以下) --- */
  @media (max-width: 700px) {
    .problems-section { 
      padding: 50px 15px; 
    }

    /* タイトルの1行化調整 */
    .section-title-problem { 
      font-size: 1.1rem; 
      gap: 6px;
      margin-bottom: 30px;
      letter-spacing: -0.02em; /* 文字間を少し詰める */
    }
    .decoration { 
      font-size: 1.1rem; 
    }
    
    /* グリッドを1列に */
    .problems-grid {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .problem-item { 
      padding: 16px;
      align-items: center; 
    }
    .problem-item p { 
      font-size: 1rem; 
    }
    .check { 
      font-size: 1.2rem; 
    }
    
    /* バナーの完全1行化調整 */
    .solution-banner {
      font-size: 0.9rem; /* 1.15remから縮小して画面内に収める */
      margin-top: 30px;
      padding: 15px 0; /* 左右のパディングを0にして文字スペースを確保 */
      width: 100%;
      border-width: 2px;
      letter-spacing: -0.03em; /* わずかに詰めて確実に入れる */
    }
  }
</style>