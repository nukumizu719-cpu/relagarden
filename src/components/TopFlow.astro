---
const steps = [
  { 
    id: "01", 
    title: "整地", 
    highlightColor: "#aee0dd", 
    desc: "雑草処理⇒水勾配墨出し山砂で整地⇔転圧を3、4回繰り返す。弊社のこだわりは、長尺の定規で限りなく凹凸がない整地です！",
    img: "/images/seiti.png",
    prices: [
      { label: "雑草なし", val: "1,500円/㎡" },
      { label: "雑草あり", val: "2,000円/㎡" },
      { label: "天然芝、砂利", val: "2,500円/㎡" },
      { label: "残土処分", val: "10,000円/㎡" },
      { label: "天然芝処分", val: "20,000円/㎡" }
    ]
  },
  { 
    id: "02", 
    title: "防草シート施工", 
    highlightColor: "#fff59d", 
    desc: "耐候年数10～13年の防草シートを1～2mm余尺を確保し、隅から光が入らないように丁寧に敷いていきます。",
    img: "/images/bososeet.png",
    note: "※耐候年数13～14年",
    prices: [
      { label: "人工芝用", val: "400円/㎡" },
      { label: "砂利敷用", val: "500円/㎡" },
      { label: "厄介な雑草用", val: "600円/㎡" },
      { label: "シート張り工賃", val: "500円/㎡" }
    ]
  },
  { 
    id: "03", 
    title: "人工芝施工", 
    highlightColor: "#a5d6a7", 
    desc: "お子様・愛犬の遊び場・雑草対策・景観用それぞれ用途に合った人工芝をご提案します。継ぎ目を感じさせない美しさが自慢です。",
    img: "/images/jinkoshiba.png",
    note: "※耐候年数別",
    prices: [
      { label: "3～5年", val: "1,000～2,000円/㎡" },
      { label: "7～8年", val: "2,500～3,000円/㎡" },
      { label: "10年以上", val: "4,000～5,000円/㎡" },
      { label: "人工芝設置工賃", val: "2,500円/㎡" }
    ]
  }
];
---

<section class="flow-section">
  <div class="container">
    <h2 class="section-title">施工の流れと料金目安</h2>

    <div class="flow-grid">
      {steps.map((step) => (
        <div class="flow-column">
          <div class="flow-header">
            <span class="flow-number">{step.id}</span>
            <h3 class="flow-step-title">
              <span class="marker" style={`background: linear-gradient(transparent 65%, ${step.highlightColor} 65%);`}>
                {step.title}
              </span>
            </h3>
          </div>
          
          <div class="flow-card">
            <div class="flow-text-area">
              <p>{step.desc}</p>
            </div>
            
            <div class="flow-image-area">
              <img src={step.img} alt={step.title} loading="lazy" />
            </div>
            
            <div class="flow-price-area">
              <div class="price-note-wrapper">
                {step.note ? <p class="price-note">{step.note}</p> : <div class="price-note-spacer"></div>}
              </div>
              <ul class="price-list">
                {step.prices.map((p) => (
                  <li>
                    <span class="p-label">● {p.label}</span>
                    <span class="p-val">{p.val}</span>
                  </li>
                ))}
              </ul>
            </div>
          </div>
        </div>
      ))}
    </div>

    <div class="total-minimal-summary">
      <div class="summary-line">
        <div class="price-row">
          <span class="s-label">施工金額目安</span>
          <span class="s-price">8,000<small>円</small> ～ 12,000<small>円/㎡</small></span>
        </div>
        <span class="s-warranty">※5年間の施工保証付き！</span>
      </div>
    </div>
  </div>
</section>

<style>
  /* 基本設定 (PC) */
  .flow-section {
    padding: 80px 0;
    background-color: #ffffff;
    font-family: 'Helvetica Neue', Arial, 'Hiragino Kaku Gothic ProN', 'Hiragino Sans', Meiryo, sans-serif;
    overflow: hidden;
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }
  .section-title {
    text-align: center;
    font-size: 2.2rem;
    font-weight: 800;
    margin-bottom: 60px;
    color: #333;
    white-space: nowrap;
  }

  .flow-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
    margin-bottom: 50px;
    align-items: stretch; /* カードの高さを一番高いものに揃える */
  }

  .flow-column {
    display: flex;
    flex-direction: column;
  }

  .flow-header {
    text-align: center;
    margin-bottom: 25px;
  }

  .flow-number {
    display: block;
    font-family: serif;
    font-size: 2.2rem;
    color: #2e7d32;
    font-style: italic;
    line-height: 1;
    margin-bottom: 5px;
    opacity: 0.5;
  }

  .flow-step-title {
    font-size: 1.5rem;
    font-weight: 800;
    margin: 0;
  }

  .marker {
    display: inline-block;
    padding: 0 10px;
  }

  /* カード全体の構造補正 */
  .flow-card {
    display: flex;
    flex-direction: column;
    height: 100%;
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid #edf2f4;
    box-shadow: 0 10px 20px rgba(0,0,0,0.02);
  }

  /* 補正1: テキストエリアの高さを固定して揃える */
  .flow-text-area {
    background-color: #f8fafb;
    padding: 25px;
    min-height: 140px; /* テキスト量の差を吸収 */
    display: flex;
    align-items: center;
    border-bottom: 1px solid #edf2f4;
  }

  .flow-text-area p {
    font-size: 0.95rem;
    line-height: 1.8;
    color: #444;
    margin: 0;
  }

  /* 補正2: 画像アスペクト比を16:10に強制固定 */
  .flow-image-area {
    width: 100%;
    aspect-ratio: 16 / 10;
    overflow: hidden;
    background: #f0f0f0;
  }

  .flow-image-area img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* 比率が違っても切り抜いて美しく収める */
    display: block;
  }

  /* 補正3: 料金エリアの高さを伸ばして下端を揃える */
  .flow-price-area {
    padding: 25px;
    background-color: #fff;
    flex-grow: 1; /* 余ったスペースを埋める */
    display: flex;
    flex-direction: column;
  }

  /* 補正4: 注釈の有無によるズレを防ぐ */
  .price-note-wrapper {
    min-height: 30px;
    margin-bottom: 15px;
  }

  .price-note {
    font-size: 0.8rem;
    font-weight: bold;
    color: #2e7d32;
    margin: 0;
  }

  .price-note-spacer {
    height: 30px;
  }

  .price-list {
    list-style: none;
    padding: 0;
    margin: 0;
    margin-top: auto; /* リストを常にエリアの最下部に配置 */
  }

  .price-list li {
    display: flex;
    justify-content: space-between;
    padding: 12px 0;
    border-bottom: 1px solid #f1f1f1;
    font-size: 0.9rem;
  }

  .price-list li:last-child {
    border-bottom: none;
  }

  .p-label { color: #666; }
  .p-val { font-weight: 900; color: #111; }

  /* 合計目安エリア (PC) */
  .total-minimal-summary {
    border-top: 2px solid #2e7d32;
    padding-top: 40px;
    text-align: center;
    margin-top: 20px;
  }

  .summary-line {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
  }

  .price-row {
    display: flex;
    align-items: baseline;
    gap: 15px;
  }

  .s-label { font-size: 1.1rem; color: #666; font-weight: bold; }
  .s-price { font-size: 2.2rem; color: #2e7d32; font-weight: 900; white-space: nowrap; }
  .s-price small { font-size: 1.1rem; margin: 0 2px; }
  .s-warranty {
    font-size: 0.9rem;
    color: #2e7d32;
    background-color: #f1f8f3;
    padding: 6px 18px;
    border-radius: 50px;
    font-weight: bold;
  }

  /* スマホ対応 */
  @media (max-width: 900px) {
    .flow-section { padding: 50px 0; }
    .section-title {
      font-size: 1.3rem;
      margin-bottom: 40px;
      letter-spacing: -0.02em;
    }
    .flow-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .flow-text-area {
      min-height: auto;
    }
    .price-note-wrapper {
      min-height: auto;
    }
    .price-note-spacer {
      display: none;
    }
    .summary-line {
      flex-direction: column;
      gap: 12px;
    }
    .price-row {
      width: 100%;
      justify-content: center;
      gap: 8px;
    }
    .s-label { font-size: 0.85rem; }
    .s-price { font-size: 1.4rem; }
  }
</style>